<nav class="menu">
    <ul>
      <li><a href="<%= root_path %>">Home</a></li>
      <li><a href="<%= root_path %>#about">About</a></li>
      <li><a href="<%= root_path %><%= root_path %>#skills">Skills</a></li>
      <li><a href="<%= root_path %>#carousel-projects">Projects</a></li>
      <li><a href="<%= root_path %>#contact">Contact</a></li>
    </ul>
  </nav>
<div class="background-show show">
  <div class="container white-background">
    <h1><%= @project.title %></h1>
    <div class="project-infos">
      <img src="<%= cl_image_path @project.photo.key, radius: 20 %>" alt="project">
      <div class="project-description">
        <h2><%= @project.category.name %></h2>
        <p><i class="fa-solid fa-caret-right"></i> <%= @project.description %></p>
        <p><i class="fa-solid fa-caret-right"></i> Projet réalisé en <%= ConvertDate.new(date: @project.creation_date.to_s).call %>.</p>
        <p id="project-link"><i class="fa-solid fa-caret-right"></i> <%= link_to " Découvrir ce projet !", @project.url, class: "project_url", target: :_blank %></p>
      </div>
    </div>
  </div>
</div>
